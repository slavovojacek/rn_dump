import { StyleSheet } from 'react-native'

const Style = StyleSheet.create({
  h2: {
    color: 'black',
    fontWeight: 'bold',
    fontSize: 24,
    padding: 0,
    margin: 0
  }
})

export default Style
